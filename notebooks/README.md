# Notebooks: exploratory only

Nothing in `results/` may be produced from a notebook. Any analysis that
feeds the paper must live in `src/` under version control, run through the
harness, and log the protocol lock hash. (Frozen-protocol rule — closes the
researcher-degrees-of-freedom side door.)
